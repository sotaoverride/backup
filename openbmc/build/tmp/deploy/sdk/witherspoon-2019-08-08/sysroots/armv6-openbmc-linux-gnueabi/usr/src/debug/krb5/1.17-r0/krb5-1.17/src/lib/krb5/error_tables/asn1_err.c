/*
 * et-c-asn1_err.c:
 * This file is automatically generated; please do not edit it.
 */

#include <stdlib.h>

#define N_(a) a

static const char* const text[] = {
    N_("ASN.1 failed call to system time library"),
    N_("ASN.1 structure is missing a required field"),
    N_("ASN.1 unexpected field number"),
    N_("ASN.1 type numbers are inconsistent"),
    N_("ASN.1 value too large"),
    N_("ASN.1 encoding ended unexpectedly"),
    N_("ASN.1 identifier doesn't match expected value"),
    N_("ASN.1 length doesn't match expected value"),
    N_("ASN.1 badly-formatted encoding"),
    N_("ASN.1 parse error"),
    N_("ASN.1 bad return from gmtime"),
    N_("ASN.1 non-constructed indefinite encoding"),
    N_("ASN.1 missing expected EOC"),
    N_("ASN.1 object omitted in sequence"),
    0};

struct error_table
{
    char const* const* msgs;
    long base;
    int n_msgs;
};
struct et_list
{
    struct et_list* next;
    const struct error_table* table;
};
extern struct et_list* _et_list;

const struct error_table et_asn1_error_table = {text, 1859794432L, 14};

static struct et_list link = {0, 0};

void initialize_asn1_error_table_r(struct et_list** list);
void initialize_asn1_error_table(void);

void initialize_asn1_error_table(void)
{
    initialize_asn1_error_table_r(&_et_list);
}

/* For Heimdal compatibility */
void initialize_asn1_error_table_r(struct et_list** list)
{
    struct et_list *et, **end;

    for (end = list, et = *list; et; end = &et->next, et = et->next)
        if (et->table->msgs == text)
            return;
    et = malloc(sizeof(struct et_list));
    if (et == 0)
    {
        if (!link.table)
            et = &link;
        else
            return;
    }
    et->table = &et_asn1_error_table;
    et->next = 0;
    *end = et;
}
